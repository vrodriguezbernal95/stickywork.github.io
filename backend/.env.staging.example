# Ejemplo de variables de entorno para STAGING
# Copia estas variables en Railway → Staging Service → Variables

# Entorno
NODE_ENV=staging

# Base de datos (usar la misma de producción por ahora)
DATABASE_URL=mysql://root:password@host:port/railway

# JWT (puedes usar el mismo secret o uno diferente)
JWT_SECRET=tu-jwt-secret-aqui
JWT_EXPIRES_IN=7d

# Anthropic API (opcional: usar key de prueba diferente)
ANTHROPIC_API_KEY=sk-ant-api03-...

# Frontend URL (para CORS)
FRONTEND_URL=http://localhost:3000

# Modo staging (para identificar en logs)
STAGING_MODE=true

# Email (opcional: usar servicio de prueba)
EMAIL_SERVICE=gmail
EMAIL_USER=test@example.com
EMAIL_PASSWORD=password

# Puerto (Railway lo asigna automáticamente)
PORT=3000
